# From `turso db show konomi --url` and `turso db tokens create konomi`
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Added later, once OAuth is wired up (Auth.js secret, provider keys, etc.)
# AUTH_SECRET=